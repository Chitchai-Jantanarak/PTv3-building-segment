# Auto-generated by `python main.py task=mae task.loss.calibrate=true`.
# Until calibration runs on the training host, this file is intentionally
# empty -- the resolver in MAEModel._build_loss_weights will fall through
# to the default (all 1.0).
#
# To force specific weights without calibration, set loss.feature_weights
# in configs/task/mae.yaml -- that overrides this file.

feature_weights: {}
