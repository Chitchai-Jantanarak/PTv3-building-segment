- task:
  name: mae
  module: src.train.mae

- data:
  split: train
  voxel_size: 0.04
  max_points: 80000
  batch_size: 2
  num_workers: 0

- model:
  mask_ratio: 0.7

- training:
  epochs: 50
  lr: 1e-3
  weight_decay: 0.05
